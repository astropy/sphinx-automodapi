[tox]
envlist = py{27,36,37}-test
requires = pip >= 18.0
           setuptools >= 30.3.0

[testenv]
changedir = {envtmpdir}
extras =
    test: test
commands =
    test: pytest --pyargs sphinx_automodapi --cov sphinx_automodapi -v
